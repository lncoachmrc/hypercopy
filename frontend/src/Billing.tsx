import './billing.css';
import {useEffect,useMemo,useState} from 'react';import {get,post} from './api';import {detectLanguage,htmlLocaleByLanguage} from './i18n';
type Sub={entitled:boolean;status:string;plan:string|null;commercial_plan?:string|null;period_end:string|null;limits:Record<string,unknown>;portfolio_equity?:number|null;portfolio_limit_usd?:number|null;portfolio_limit_exceeded?:boolean};
type Plan={slug:string;name:string;portfolio_up_to_usd:number;monthly_usd:number;yearly_usd:number;yearly_monthly_equivalent_usd:number;description:string;monthly_checkout_available:boolean;yearly_checkout_available:boolean};
type Trial={days:number;portfolio_up_to_usd:number;max_positions:number;max_notional_per_trade_usd:number;max_multiplier:number;card_required:boolean};
type Catalog={currency:string;yearly_discount_pct:number;minimum_portfolio_usd:number;trial:Trial;plans:Plan[];overage:{included_portfolio_usd:number;excess_fee_annual_pct:number;custom_terms_from_usd:number};legacy_display_map:Record<string,string>};
type Period='monthly'|'yearly';
const usd=(n:number,decimals=0)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',minimumFractionDigits:decimals,maximumFractionDigits:decimals}).format(n);
const localDate=(value:string|null)=>value?new Intl.DateTimeFormat(htmlLocaleByLanguage[detectLanguage()],{day:'2-digit',month:'2-digit',year:'numeric'}).format(new Date(value)):null;
export default function Billing(){const [s,setS]=useState<Sub|null>(null),[catalog,setCatalog]=useState<Catalog|null>(null),[period,setPeriod]=useState<Period>('monthly'),[busy,setBusy]=useState(''),[error,setError]=useState('');useEffect(()=>{void Promise.all([get<Sub>('/subscription'),get<Catalog>('/subscription/plans')]).then(([sub,plans])=>{setS(sub);setCatalog(plans)}).catch(e=>setError(e instanceof Error?e.message:'Errore caricamento piani'))},[]);const current=useMemo(()=>s?.commercial_plan||s?.plan||null,[s]);const checkout=async(plan:string)=>{setError('');setBusy(plan);try{const r=await post<{url:string}>('/subscription/checkout',{plan,billing_period:period});location.href=r.url}catch(e){setError(e instanceof Error?e.message:'Checkout non disponibile')}finally{setBusy('')}};const portal=async()=>{setError('');try{const r=await post<{url:string}>('/subscription/portal');location.href=r.url}catch(e){setError(e instanceof Error?e.message:'Customer Portal non disponibile')}};const trial=catalog?.trial;return <><div className="title"><div><h1>Piani TRAXION</h1><p>Il piano dipende dal valore del portafoglio operativo. Tutti i limiti sono applicati anche lato backend.</p></div>{s?.plan&&s.plan!=='trial'&&<button onClick={()=>void portal()}>Customer Portal</button>}</div>{error&&<div className="alert error billing-alert">{error}</div>}{trial&&<section className={`trial-offer ${current==='trial'?'active':''}`}><div><div className="trial-kicker">PROVA GRATUITA</div><h2>{trial.days} giorni per provare TRAXION</h2><p>Accesso completo a Dashboard, Risk Engine Basic + Pro e trading ibrido automatizzato su Hyperliquid, entro limiti pensati per una prova reale ma controllata.</p><div className="trial-features"><span>Portfolio fino a {usd(trial.portfolio_up_to_usd)}</span><span>Max {trial.max_positions} posizioni</span><span>Max {usd(trial.max_notional_per_trade_usd)} / trade</span><span>Intensità fino a {trial.max_multiplier}×</span><span>{trial.card_required?'Carta richiesta':'Nessuna carta richiesta'}</span></div></div><div className="trial-status"><strong>{current==='trial'?'TRIAL ATTIVO':'GRATIS'}</strong><span>{current==='trial'&&s?.period_end?`Termina il ${localDate(s.period_end)}`:'Si attiva automaticamente al primo accesso'}</span></div></section>}<section className="panel billing-current"><div><span>Piano attuale</span><strong>{current==='starter'?'Starter':current==='plus'?'Plus':current==='pro_10k'?'Pro':s?.plan||'—'}</strong></div><div><span>Stato</span><strong>{s?.status||'—'}</strong></div><div><span>Entitlement</span><strong className={s?.entitled?'up':'down'}>{s?.entitled?'ATTIVO':'NON ATTIVO'}</strong></div>{s?.portfolio_equity!=null&&<div><span>Portafoglio rilevato</span><strong>{usd(s.portfolio_equity,2)}</strong></div>}</section><div className="billing-cycle"><button className={period==='monthly'?'active':''} onClick={()=>setPeriod('monthly')}>Mensile</button><button className={period==='yearly'?'active':''} onClick={()=>setPeriod('yearly')}>Annuale</button><span>Risparmia {catalog?.yearly_discount_pct??50}%</span></div><div className="pricing-grid">{catalog?.plans.map(p=>{const annual=period==='yearly';const value=annual?p.yearly_monthly_equivalent_usd:p.monthly_usd;const configured=annual?p.yearly_checkout_available:p.monthly_checkout_available;const isCurrent=current===p.slug;return <section className={`pricing-card ${p.slug==='plus'?'featured':''}`} key={p.slug}><div className="pricing-cap">PORTAFOGLIO FINO A {usd(p.portfolio_up_to_usd)}</div><h2>{p.name}</h2><p className="pricing-description">{p.description}</p><div className="pricing-price"><strong>{usd(value,value%1?2:0)}</strong><span>/mese</span></div><div className="pricing-billed">{annual?`fatturato ${usd(p.yearly_usd)}/anno`:'fatturato mensilmente'}</div><ul className="pricing-features"><li>Trading ibrido multi-asset su Hyperliquid</li><li>Strategia sorgente sviluppata con analisti + sistemi AI</li><li>Risk Engine Basic + Pro</li><li>Esecuzione automatizzata, PnL e reconciliation</li></ul><button className="pricing-cta" disabled={isCurrent||!configured||busy===p.slug} title={!configured?'Configura il relativo Stripe Price ID per abilitare il checkout':''} onClick={()=>void checkout(p.slug)}>{isCurrent?'Piano attuale':busy===p.slug?'Apertura checkout…':configured?'Scegli piano':'Checkout da configurare'}</button></section>})}</div>{catalog&&<div className="pricing-extras"><section className="panel"><h2>Portafoglio oltre {usd(catalog.overage.included_portfolio_usd)}?</h2><p>Rimani sul piano Pro e si applica una fee annua del <b>{catalog.overage.excess_fee_annual_pct}%</b> esclusivamente sul valore di portafoglio oltre {usd(catalog.overage.included_portfolio_usd)}.</p><small>La componente di eccedenza richiede il billing di produzione dedicato prima del lancio mainnet.</small></section><section className="panel"><h2>Portafoglio oltre {usd(catalog.overage.custom_terms_from_usd)}?</h2><p>Condizioni personalizzate, fee ridotte e supporto dedicato.</p><small>Contatto commerciale dedicato.</small></section></div>}<p className="pricing-footnote">Prezzi in USD. Portafoglio minimo indicativo: {usd(catalog?.minimum_portfolio_usd??500)}. In TESTNET/SUPERADMIN i limiti commerciali possono essere bypassati esclusivamente per validare la pipeline.</p></>}
