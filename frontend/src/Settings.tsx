import {useEffect,useState} from 'react';
import {del,get,post,put} from './api';
import {useAuth} from './auth';
type Me={auth_wallet:string;copy_state:string;risk_state:string;master_network:string;follower_network:string;trading_account:null|{account_address:string;network:string;agent_address:string;agent_name:string;credential_status:string;expires_at:string|null}};
type Risk={multiplier:string;max_notional_per_trade:string;max_total_exposure:string;max_asset_exposure:string;max_leverage:string;max_positions:number;max_drawdown_pct:string;max_daily_loss_pct:string;min_notional:string;max_slippage_bps:number;close_only:boolean;allow_assets:string[];block_assets:string[]};
export default function Settings(){const {refresh}=useAuth();const [me,setMe]=useState<Me|null>(null),[risk,setRisk]=useState<Risk|null>(null),[agent,setAgent]=useState(''),[key,setKey]=useState(''),[msg,setMsg]=useState('');const load=async()=>{setMe(await get('/me'));setRisk(await get('/risk-profile'))};useEffect(()=>{void load()},[]);const link=async()=>{setMsg('Verifica API Wallet…');try{await post('/trading-account',{agent_address:agent,agent_private_key:key});setKey('');setMsg('API Wallet verificato e chiave cifrata.');await load()}catch(e){setMsg(e instanceof Error?e.message:'Errore')}};const save=async()=>{if(!risk)return;await put('/risk-profile',risk);setMsg('Profilo rischio salvato.')};const setShadow=async()=>{await post('/copy/shadow');await load();await refresh();setMsg('Modalità SHADOW attiva: i target vengono calcolati ma non vengono inviati ordini.')};return <><div className="title"><div><h1>Configurazione</h1><p>Il wallet con cui accedi a HyperCopy è anche il tuo account Hyperliquid follower. Per autorizzare gli ordini usa soltanto un API Wallet Hyperliquid nominato <code>hypercopy</code>.</p></div></div><div className="cols"><section className="panel"><h2>Account copytrading</h2>{me&&<dl><dt>Rete Master</dt><dd>{me.master_network.toUpperCase()}</dd><dt>Rete Follower</dt><dd>{me.follower_network.toUpperCase()}</dd><dt>Modalità copy</dt><dd>{me.copy_state}</dd></dl>}{me?.trading_account?<><dl><dt>Wallet cliente / account follower</dt><dd>{me.trading_account.account_address}</dd><dt>Rete account follower</dt><dd>{me.trading_account.network.toUpperCase()}</dd><dt>API Wallet / Agent</dt><dd>{me.trading_account.agent_address}</dd><dt>Nome Agent</dt><dd>{me.trading_account.agent_name||'hypercopy'}</dd><dt>Stato credenziale</dt><dd>{me.trading_account.credential_status}</dd><dt>Scadenza</dt><dd>{me.trading_account.expires_at?new Date(me.trading_account.expires_at).toLocaleDateString():'—'}</dd></dl><p className="muted">La private key del wallet principale non viene mai richiesta da HyperCopy.</p><button className="danger" onClick={async()=>{await del('/trading-account');await load()}}>Scollega API Wallet</button></>:<><dl><dt>Wallet cliente / account follower</dt><dd>{me?.auth_wallet||'Caricamento…'}</dd></dl><p className="muted">Questo indirizzo deriva dal wallet con cui hai effettuato l'accesso e non può essere sostituito con un altro account.</p><label>API Wallet Address<input value={agent} onChange={e=>setAgent(e.target.value)} placeholder="0x…" autoComplete="off"/></label><label>API Wallet Private Key<input type="password" autoComplete="off" value={key} onChange={e=>setKey(e.target.value)} placeholder="0x…"/></label><p className="muted">Inserisci l'indirizzo e la private key dello stesso API Wallet Hyperliquid. HyperCopy controllerà che la chiave generi esattamente quell'indirizzo e che l'Agent sia autorizzato sul tuo account.</p><button className="primary" onClick={()=>void link()} disabled={!agent||!key}>Verifica e collega</button></>}<hr/><div className="actions"><button onClick={async()=>{await post('/copy/pause');await load();await refresh()}} disabled={me?.copy_state==='PAUSED'}>Pausa</button><button className="primary" onClick={()=>void setShadow()} disabled={!me?.trading_account||me?.copy_state==='SHADOW'}>Modalità SHADOW</button><button onClick={async()=>{if(confirm(`Attivare il copytrading con invio ordini sulla rete ${me?.follower_network?.toUpperCase()||''}?`)){await post('/copy/resume');await load();await refresh()}}} disabled={!me?.trading_account||me?.copy_state==='ACTIVE'}>Attiva copy {me?.follower_network?.toUpperCase()||''}</button><button className="danger" onClick={async()=>{if(confirm('Generare ordini reduce-only per chiudere tutte le posizioni gestite?')){await post('/copy/close-positions',{confirmation:'CLOSE',reason:'User requested close all'});setMsg('Chiusure accodate.')}}}>Chiudi posizioni</button></div><p className="muted">SHADOW calcola target e delta senza inviare ordini. "Attiva copy" abilita invece l'esecuzione sulla rete follower indicata.</p></section><section className="panel"><h2>Risk Engine</h2>{risk&&<div className="formgrid"><Num label="Multiplier" value={risk.multiplier} set={v=>setRisk({...risk,multiplier:v})}/><Num label="Max / trade $" value={risk.max_notional_per_trade} set={v=>setRisk({...risk,max_notional_per_trade:v})}/><Num label="Max exposure $" value={risk.max_total_exposure} set={v=>setRisk({...risk,max_total_exposure:v})}/><Num label="Max asset $" value={risk.max_asset_exposure} set={v=>setRisk({...risk,max_asset_exposure:v})}/><Num label="Max leverage" value={risk.max_leverage} set={v=>setRisk({...risk,max_leverage:v})}/><Num label="Max drawdown %" value={risk.max_drawdown_pct} set={v=>setRisk({...risk,max_drawdown_pct:v})}/><Num label="Daily loss %" value={risk.max_daily_loss_pct} set={v=>setRisk({...risk,max_daily_loss_pct:v})}/><Num label="Slippage bps" value={String(risk.max_slippage_bps)} set={v=>setRisk({...risk,max_slippage_bps:Number(v)})}/><label className="check"><input type="checkbox" checked={risk.close_only} onChange={e=>setRisk({...risk,close_only:e.target.checked})}/> Close-only</label><button className="primary" onClick={()=>void save()}>Salva limiti</button></div>}</section></div>{msg&&<div className="toast">{msg}</div>}</>}
function Num({label,value,set}:{label:string;value:string;set:(v:string)=>void}){return <label>{label}<input type="number" step="any" value={value} onChange={e=>set(e.target.value)}/></label>}
